Steps to run backup script

1. `chmod +x /home/atharva/test/backup.sh`
2. `./backup.sh`

Steps to run restore script

1. `chmod +x /home/atharva/test/restore.sh`
2. `./restore.sh`


Auto run backup script on startup

`sudo nano /etc/systemd/system/dokploy-backup.service`